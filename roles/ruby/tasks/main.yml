---
- stat: path={{ ruby_path }}
  register: ruby_installed

- name: Install ruby
  include: install.yml
  when: ruby_installed.stat.exists == False
  tags:
    - install
    - ruby
    - install:ruby

# - name: Configure ruby
#   include: configure.yml
#   tags:
#     - configure
#     - ruby
#     - configure:ruby
