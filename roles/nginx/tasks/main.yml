---
- name: Install nginx
  include: install.yml
  tags:
    - install
    - nginx
    - install:nginx

- name: Configure nginx
  include: configure.yml
  tags:
    - configure
    - nginx
    - configure:nginx
