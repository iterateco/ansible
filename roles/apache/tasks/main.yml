---
- name: Install apache
  include: install.yml
  tags:
    - install
    - apache
    - install:apache

- name: Configure apache
  include: configure.yml
  tags:
    - configure
    - apache
    - configure:apache
