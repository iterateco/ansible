# currently only supports php
---
- name: Install aws-elasticache
  include_tasks: install.yml
  tags:
    - install
    - aws-elasticache
    - install:aws-elasticache

- name: Configure aws-elasticache
  include_tasks: configure.yml
  tags:
    - configure
    - aws-elasticache
    - configure:aws-elasticache
